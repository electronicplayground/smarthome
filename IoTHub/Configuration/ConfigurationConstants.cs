namespace IoTHub {
    internal class ConfigurationConstants {
        internal const string MQTT_TOPIC = "MQTT_TOPIC";
        internal const string MQTT_BROKER = "MQTT_BROKER";
        internal const string MQTT_PORT = "MQTT_PORT";
    }
}